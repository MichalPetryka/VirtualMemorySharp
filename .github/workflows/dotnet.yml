name: .NET Tests
on: [push, pull_request]
jobs:
  build:
    runs-on: [windows-latest, ubuntu-latest, macos-latest, macos-15-intel]
    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 10.0
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity verbose
